name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Run Tests
        run: go test -v ./...

  pkgsafe-gate:
    name: PkgSafe Package Gate Self-Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run PkgSafe Dependency Gate
        uses: ./
        with:
          lockfile: testdata/npm/package-lock.json
          fail-on: block
          changed-only: false
          sandbox: false
          upload-sarif: false
